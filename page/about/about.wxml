<!--page/about/about.wxml-->
<view>名字：{{name}}</view>
<view>省份：{{province}}</view>



<view wx:for="{{goods}}">{{item.name}}</view>
<view wx:for="{{goods}}">{{item.name}}</view>
<view wx:for="{{goods}}">{{item.name}}</view>
<view wx:for="{{goods}}">{{item.name}}</view>
