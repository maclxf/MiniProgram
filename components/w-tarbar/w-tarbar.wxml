<!--components/w-tarbar/w-tarbar.wxml-->
<view class="container">
  <block wx:for="{{types}}" wx:for-index="index" wx:key="{{index}}">
    <view class="sub {{current == index ? 'active' : ''}}" bindtap="clickItem" data-index="{{index}}">
      <text>{{item}}</text>
    </view>
  </block>
</view>
